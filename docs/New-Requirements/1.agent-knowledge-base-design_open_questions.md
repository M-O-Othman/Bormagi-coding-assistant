# Open Questions: Agent Knowledge Base Design v1.0 Implementation

Before proceeding with the implementation plan outlined in `1.agent-knowledge-base-design_implemenattion_plan.md`, the following questions need clarification:

1. **Infrastructure Assumptions:**
   - The design specifies using ChromaDB and Ollama running locally via Docker (`docker-compose.yaml`). Are you currently running Docker locally, and should I add `docker-compose.yaml` to the root of the project to enforce this dependency? 
   **Answer:** the design is just a gideline , it needs to be ported into our project implemenattion we are building a vs code extention so a fully fledged database implemenattion might not be required , a simple file based and vector based liberaries might be sifficient , use your best judgement to check what technology will be suitable , we are running an extension of VS code so there is no docker incolved , the LLMs are engaged through the defined agents , no additional LLM connections are required ,OLLama is not in scope
   - Since Ollama requires downloading a model (`nomic-embed-text`), should I automate this model download via setup scripts, or assume the user handles it manually?
   **Answer:**find a simple embedding liberary that does the job , the simpler the better

2. **ChromaDB vs Existing VectorStore:**
   - I see a rudimentary `VectorStore.ts` currently exists. Should I completely rip out `VectorStore.ts` and replace the persistence mechanisms with calls to the external API of the ChromaDB container, or maintain `VectorStore.ts` as an interface wrapping ChromaDB?
   **Answer:** use your judgement , the user should be given the choice to store data either in folders (RAG) or in vovetors through embedding , Agent *MUST* consult the relevant knowledge embedded or present in the folder before answering user queries.

3. **Message Bus Backend:**
   - The design states "File-based for local dev / Redis Streams for production." Since this is a VS Code extension intended for self-hosted/local use, should we build the file-based message bus using `chokidar` for now, or jump straight to requiring Redis?
   **Answer:**Redis is too much overkill , fiind something simple that does the job ; the size of the agent knowledge data is not expected to be huge (maybe equavelent to ten technical books)

4. **Multi-Agent Runtime Control:**
   - Currently, agents are run individually via VS Code commands in Webviews (`MainPanel.ts`). When an agent delegates a task via the new message bus, does the receiving agent automatically spin up in the background (headless), or does it spawn a new UI panel for the user to observe?
   **Answer:**Agent must prepare the reponses to user queries based on the stored konledge (RAG and Embedded), design the flow accordingly

5. **Tool Manifest Migration:**
   - The project currently uses a global MCP host where all installed tools are accessible based on explicit user prompt injections. Should I restrict this based on the new agent-specific `tool-manifest.yaml` (meaning some agents lose access to tools they currently have), or bypass Phase 4 of the implementation for now?
   **Answer:** skip for now
